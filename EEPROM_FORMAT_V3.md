# NAPI Board EEPROM Format v3

24C02, 256 байт, I2C1, адрес `0x50`. Все многобайтные числа little endian.
Определённые поля занимают 126 байт; резерв `0x7E..0xFF` — 130 байт.

| Offset | Размер | Поле | Формат |
| --- | --- | --- | --- |
| `0x00` | 4 | magic | ASCII `NAPI` |
| `0x04` | 1 | format_version | 3 |
| `0x05` | 1 | platform_id | uint8; 1 = rk3308 |
| `0x06` | 4 | product_id | uint32 |
| `0x0A` | 2 | product_rev | uint16 |
| `0x0C` | 4 | interface_mask | uint32 |
| `0x10` | 4 | serial_number | uint32; 0 = не назначен |
| `0x14` | 1 | mfg_year | год минус 2000 |
| `0x15` | 1 | mfg_month | 1–12 |
| `0x16` | 1 | mfg_day | 1–31, с проверкой календарной даты |
| `0x17` | 1 | mac_count | 0–8 |
| `0x18` | 32 | board_name | UTF-8, дополнение нулями |
| `0x38` | 48 | mac[8][6] | восемь бинарных MAC |
| `0x68` | 1 | proc_id_type | тип источника ID |
| `0x69` | 1 | proc_id_len | число значащих байт, 0–16 |
| `0x6A` | 16 | proc_id | исходные байты ID, дополнение нулями |
| `0x7A` | 4 | crc32 | CRC32 байт `0x00..0x79` |
| `0x7E` | 130 | reserved | резерв для будущих расширений |

Дата `00 00 00` означает «не задана». Serial принадлежит экземпляру платы,
не участвует в генерации MAC и не меняется при замене процессора.
Комментарий профиля не хранится в EEPROM.

## Interface mask

| Bit | Назначение |
| --- | --- |
| 0 | Reserved |
| 1 | USB Host |
| 2 | SPI1 |
| 3 | SPI2 |
| 4 | UART1 |
| 5 | UART2 |
| 6 | UART3 |
| 7 | W5500 SPI1 |
| 8 | W5500 SPI2 |
| 9 | I2C0 |
| 10 | I2C1 |
| 11 | I2C3 |
| 12 | UART4 |
| 13 | RTC DS1307 |
| 14 | RTC DS1338 |
| 15 | RTC DS3231 |
| 16–31 | Reserved |

RTC bits 13–15 взаимоисключающие. Физическая EEPROM подключена к I2C1.
По уточнению пользователя включение I2C1 в interface_mask независимо:
запись EEPROM не требует bit10 и не включает его автоматически.
Для аппаратного доступа необходимо доступное устройство EEPROM в Linux.
EEPROM overlay автоматически не включается.

## Processor binding

Тип 0: `proc_id_len=0`, все 16 байт — нули. Тип 1: RK3308 OTP,
`proc_id_len=5`, ровно пять исходных байт из
`/sys/bus/nvmem/devices/rockchip-otp0/nvmem`, offset 20, затем 11 нулей.
Чтение использует exclusive advisory flock с таймаутом 2 секунды.
ID из пяти нулей или FF недопустим. Типы 2–255 зарезервированы и текущей
реализацией не поддерживаются. Дополнение после `proc_id_len` всегда нулевое.

Состояния сравнения: `NOT BOUND`, `MATCH`, `PROCESSOR MISMATCH`,
`OTP UNAVAILABLE`. Несовпадение не заменяет сохранённый ID: оператор должен
явно выбрать перепривязку. Она сохраняет serial и MAC.

## MAC slots

MAC1 `0x38`: Native Ethernet; MAC2 `0x3E`: W5500 SPI1;
MAC3 `0x44`: W5500 SPI2; MAC4 `0x4A`: USB Ethernet #1;
MAC5 `0x50`: USB Ethernet #2; MAC6 `0x56`, MAC7 `0x5C`, MAC8 `0x62`: Reserved.
Номера не сдвигаются при отсутствии интерфейсов.

Генерация использует `SHA256(b"NAPI-MAC-v1\x00" + otp)`, первые пять байт,
обнуление последних трёх бит пятого байта; MAC — `02` + эти пять байт,
номер слота 0–7 в младших трёх битах последнего октета.
Runtime использует сохранённые MAC, а не повторную генерацию из proc_id.
`set_native_eth_mac=false` — отдельная настройка приложения вне EEPROM.

Serial/date/MAC/proc_id — данные экземпляра, их нет в `boards.yaml`.

## Совместимость

Версию нужно прочитать до разбора полей и проверки CRC.
В v2 CRC находится в `0x68..0x6B` и покрывает `0x00..0x67`;
это не поля привязки процессора. Миграция v2 → v3 выполняется отдельно,
с подтверждением, резервной копией и read-back, и оставляет ID незаданным.
Любое изменение защищённых полей требует пересчёта CRC.
